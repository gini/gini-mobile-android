name: SonarQube Analysis

permissions:
  contents: write
  packages: write

on:
    workflow_dispatch:
    push:
      branches: [ "*" ]
    pull_request:
      types: [ opened, edited, reopened, synchronize ]
      paths:
        - 'bank-api-library/**'


jobs:
    sonar:
      runs-on: ubuntu-latest

      steps:
        - name: Checkout
          uses: actions/checkout@v4

        - name: Set up JDK
          uses: actions/setup-java@v4
          with:
            distribution: "temurin"
            java-version: "17"

        - name: Build Bank API Library
          run: ./gradlew :bank-api-library:library:assembleDebug

        - name: SonarQube Scan
          run: ./gradlew bank-api-library:library:sonarqube
          env:
            SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}


#on:
#  workflow_dispatch:
#  push:
#  pull_request:
#    types: [ opened, edited, reopened, synchronize ]
#    paths:
#      - 'bank-api-library/**'

#jobs:
#  sonarqubeScan1:
#    name: BankApiLibrarySonarQubeScan
#    runs-on: ubuntu-latest
#    steps:
#      - uses: actions/checkout@v3
#        with:
#          fetch-depth: 0
#      - name: SonarQube Scan
#        uses: SonarSource/sonarqube-scan-action@v6
#        env:
#          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
#          SONAR_PROJECT_KEY: bank-api-library
#          SONAR_ORGANIZATION: gini
#          SONAR_HOST_URL: https://sonarcloud.io
#        with:
#          projectBaseDir: bank-api-library/

#jobs:
#  sonarqubeScan1:
#    runs-on: ubuntu-latest
#    steps:
#      - uses: actions/checkout@v3
#        with:
#          fetch-depth: 0
#      - name: Setup Java
#        uses: actions/setup-java@v3
#        with:
#          distribution: 'temurin'
#          java-version: '17'
#      - name: Build and Run Sonar
#        run: |
#          ./gradlew bank-api-library:library:sonar \
#          -Dsonar.host.url=https://sonarcloud.io
#        env:
#          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}

#name: SonarQube
#
#permissions:
#  contents: write
#  packages: write
#
#on:
#  workflow_dispatch:
#  push:
#  pull_request:
#    types: [opened, edited, reopened, synchronize]
#    paths:
#      - 'bank-api-library/**'
#jobs:
#  build:
#    name: Build and analyze
#    runs-on: ubuntu-latest
#    steps:
#      - uses: actions/checkout@v4
#        with:
#          fetch-depth: 0  # Shallow clones should be disabled for a better relevancy of analysis
#      - name: Set up JDK 17
#        uses: actions/setup-java@v4
#        with:
#          distribution: 'temurin'
#          java-version: '17'
#      - name: Cache SonarQube packages
#        uses: actions/cache@v4
#        with:
#          path: ~/.sonar/cache
#          key: ${{ runner.os }}-sonar
#          restore-keys: ${{ runner.os }}-sonar
#      - name: Cache Gradle packages
#        uses: actions/cache@v4
#        with:
#          path: ~/.gradle/caches
#          key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle') }}
#          restore-keys: ${{ runner.os }}-gradle
#      - name: Build and analyze
#        env:
#          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
#        run: ./gradlew build sonar --info