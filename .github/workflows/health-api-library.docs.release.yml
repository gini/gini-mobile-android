name: Release docs for Health API Library

permissions:
  contents: read

on:
  push:
    tags:
      - 'health-api-library;[0-9]+.[0-9]+.[0-9]+;doc**'
      - 'health-api-library;[0-9]+.[0-9]+.[0-9]+-beta[0-9][0-9];doc**'
  workflow_call:
    secrets:
      MOBILE_CI_APP_ID:
        required: true
      MOBILE_CI_APP_PRIVATE_KEY:
        required: true

jobs:
  publish:
    permissions:
      contents: write
    uses: ./.github/workflows/sdk.publish.docs.yml
    with:
      project_id: 'health-api-library'
      module_id: 'library'
      documentation_title: 'Gini Health API Library for Android'
      git_tag: ${{ github.ref }}
      artifact_name: 'health-api-library-documentation'
      artifact_path: 'health-api-library/library/build/docs'
    secrets:
      MOBILE_CI_APP_ID: ${{ secrets.MOBILE_CI_APP_ID }}
      MOBILE_CI_APP_PRIVATE_KEY: ${{ secrets.MOBILE_CI_APP_PRIVATE_KEY }}
