package net.gini.android.capture.ingredientbrand

/**
 * Screens on which the Gini ingredient brand element can be shown.
 *
 * [rawValue] is the name the `/configurations` endpoint uses in its
 * `ingredientBrandScreens` array.
 */
internal enum class IngredientBrandScreen(val rawValue: String) {
    ANALYSIS("Analysis")
}
